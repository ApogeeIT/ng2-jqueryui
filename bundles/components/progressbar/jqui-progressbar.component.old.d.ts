import * as WidgetOptions from '../../options/widget-options';
export declare class JquiProgressBarComponent implements WidgetOptions.IDisabledWidget, WidgetOptions.IMaxWidget {
    uiValue?: number;
    uiDisabled: boolean;
    uiMax: number;
}
